# Production dependencies
requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0
# cmdstanpy pinned: prophet 1.1.6 bundles a prebuilt Stan binary that
# cmdstanpy 1.3.0's stricter path validation rejects (backend fails, surfaces
# as "'Prophet' object has no attribute 'stan_backend'"). 1.2.4 loads it.
cmdstanpy==1.2.4
prophet>=1.1.6
mlflow>=2.9.0
great-expectations>=0.18.0
databricks-sql-connector>=3.0.0
delta-spark>=3.0.0
pyspark>=3.5.0